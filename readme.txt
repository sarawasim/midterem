This API uses HTTP requests to access and use data that can be used to 
GET, PUT, POST and DELETE data types. It also uses middleware that checks for 
course id and logs incoming requests with timestamps and the IP address.